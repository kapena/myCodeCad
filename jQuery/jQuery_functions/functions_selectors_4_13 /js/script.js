// Create a variable `$div`, and assign it to a `div` jQuery object using `=`. No need to use $(document).ready() or any additional code!

// variable named div assigned to an attribute (div)
var $div = $('div');
