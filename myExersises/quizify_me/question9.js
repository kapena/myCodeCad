// Question 9  

// Create the `function min(a,b)` which takes two numbers as `arguments` and `returns` the greater number using a `if/else statement`.

// Create a Function named `add` that takes 3 parameters `(result,x,y)`. Then use logic to multiply `x,y`. Pass a `string` into our `variable` named `result` as an `argument`. The string must explain what numbers we are multiplying.

function greater (a,b){
  if (a > b){
  return a + " is greater than" + "5" ;
  }

  else {
  return "b";
  }
}

console.log(greater(10,5));

// Part 2
// Create a Function named add that takes 3 parameters(result,x,y). Use logic to multiply x,y. Pass a string into our variable named `result` as an argument. The string must explain what numbers we are multiplying

function add (result,x,y){
  if (x === x){
    return + result + x * y;
  }
  
  else{
    return ;
  }
}

console.log(add("3 * 2 is ",3,2));